name: CI

on:
  push:
    branches:
      - main

jobs:
  ci:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
<<<<<<< HEAD
          pip install -r requirements.txt
          # If requirements.txt is in a different folder, adjust the path accordingly:
          # pip install -r path/to/requirements.txt
=======
          pip install -r crazy_comrades-soen341projectW2024/Car_rental_app/.github/workflows/requirements.txt
>>>>>>> a991e17a0d691b08dde68c5619ad3c7eed3d78c5
      
      # Add more CI steps here (linting, testing, etc.)

  cd:
    runs-on: windows-latest
    needs: ci
    if: github.ref == 'refs/heads/main'  # Only deploy on main branch

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x
      
      - name: Install dependencies
        run: |
<<<<<<< HEAD
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # If requirements.txt is in a different folder, adjust the path accordingly:
          # pip install -r path/to/requirements.txt
      
      # Add deployment steps here (building, deploying, etc.)
=======
          pytest crazy_comrades-soen341projectW2024/Car_rental_app/tests/test.py
>>>>>>> a991e17a0d691b08dde68c5619ad3c7eed3d78c5
